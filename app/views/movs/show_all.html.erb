<!DOCTYPE html>
<html>

<head>
  <title>Movies</title>
</head>

<body>
<h1>Movies</h1>

<a href="/movies/new_form">Add New Movie</a>

<p></p>
<% @all_movies.each do |d| %>
    <b><%= d.title %></b> (<%= d.year %>)
    <p>
      <a href="/movies/<%= d.id %>">
        View
      </a>
      &NonBreakingSpace;
      <a href="/movies/<%= d.id %>/edit_form">
        Update
      </a>
      &NonBreakingSpace;
      <a href="/delete_movie/<%= d.id %>">
        Delete
      </a>
    </p>

    <p><img src=<%= d.image_url %> width="200px"></p>

<% end %>

</body>

</html>