<!DOCTYPE html>
<html>

<head>
  <title>Actors</title>
</head>

<body>
<h1>Actors</h1>

<a href="/actors/new_form">Add New Actor</a>

<p></p>
<% @all_actors.each do |d| %>
    <b><%= d.name %></b> (born <%= d.dob %>)
    <p>
      <a href="/actors/<%= d.id %>">
        View
      </a>
      &NonBreakingSpace;
      <a href="/actors/<%= d.id %>/edit_form">
        Update
      </a>
      &NonBreakingSpace;
      <a href="/delete_actor/<%= d.id %>">
        Delete
      </a>
    </p>

    <p><img src=<%= d.image_url %> width="200px"></p>

<% end %>

</body>

</html>